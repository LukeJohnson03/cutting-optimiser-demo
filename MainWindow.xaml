<Window x:Class="CuttingOptimiserDemo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Glass Cutting Optimizer" Height="600" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="5">
            <Button Content="Run Optimization" Padding="10,5" Margin="5" Click="Optimize_Click"/>
            <TextBlock Text="Zoom:" VerticalAlignment="Center" Margin="5,0,0,0"/>
            <Slider x:Name="ZoomSlider" Minimum="0.1" Maximum="2" Value="1" Width="100" Margin="5"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <Canvas x:Name="LayoutCanvas" Background="White"
                    RenderTransformOrigin="0.5,0.5">
                <Canvas.RenderTransform>
                    <ScaleTransform ScaleX="{Binding ElementName=ZoomSlider, Path=Value}"
                                   ScaleY="{Binding ElementName=ZoomSlider, Path=Value}"/>
                </Canvas.RenderTransform>
            </Canvas>
        </ScrollViewer>

        <StatusBar Grid.Row="2">
            <StatusBarItem>
                <TextBlock x:Name="StatusText" Text="Ready"/>
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <TextBlock Text="{Binding ElementName=ZoomSlider, Path=Value, StringFormat=Zoom: {0:F1}x}"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>